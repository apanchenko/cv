{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "main",
            "cwd": "${workspaceFolder}/src/cv"
        }
    ]
}
